# 可比克校园薄脆挑战

直接双击 `index.html` 即可运行。页面使用原生 HTML、CSS、JavaScript 和 Canvas，无需安装依赖、登录或连接后台。三张图片已内嵌，单独复制这个 HTML 文件也可使用；`assets` 保留了素材副本。

## 修改校园活动信息

在 `index.html` 中搜索 `const EVENT_CONFIG`，修改其中的 `date`（时间）、`location`（地点）、`description`（内容）和 `link`（活动链接）。当前全部保留为待填写，链接为空时显示“校园活动信息即将公布，敬请期待！”。分享卡使用清楚标注的二维码预留区域，没有生成虚假二维码。

## 玩法与计分

- 一刀薄脆：保留原文件的薄片比例、透明像素判断、刀具移动与切片分离动画。切中按原公式计 100–1000 分，切空 0 分，也可进入下一关。
- 极限连切：15 秒。切中 +100 原始分，连击额外最多 +50。原始分除以 3，四舍五入并封顶 1000。
- 薄脆校准：5 次，每次最多 3 秒。四档得分为 1000 / 750 / 400 / 0，取五次平均分；超时记 0 分。
- 薯片接住：18 秒，普通薯片 +40，金色 +100，封顶 1000。最后 5 秒加速。

总分为四关最近成绩之和，满分 4000。各关最高分、历史最高总分与称号单独保存；较低成绩不会覆盖最高纪录。重新挑战一轮会清空本轮成绩，但保留历史最高与已解锁关卡。“重置挑战”需要再次确认，才会清除该网页的挑战记录。

点击、触摸、空格键均可切割或校准；连切时空格切当前土豆。接薯片支持拖动、鼠标移动及方向键。退出确认期间与页面切到后台期间暂停游戏。音效默认关闭，可点击顶部音符开启。

## 分享战绩

完成四关后点击“生成我的脆力战绩”，在本地绘制 1080 × 1440 图片。手机长按图片保存，也提供图片下载链接。没有向第三方上传成绩。

## 验证范围

`node tests/campus.test.js` 可运行无依赖逻辑测试，覆盖第一关切中/切空、四关流程、计分、超时、重玩、存档、重置确认、存储不可用、暂停与退出、Canvas 像素尺寸及分享绘制调用。

本次浏览器工具拦截了本地文件打开，因此尚未完成真实浏览器视觉、触摸与 PNG 导出验收；上述测试使用 DOM/Canvas 模拟环境，不能替代真机检查。建议正式发布前用手机微信、Safari 和桌面 Chrome 打开交付文件确认。浏览器清理站点数据、隐私模式或更换文件位置/访问域名，可能导致历史记录不可用。

原始用户素材和原 `index.html` 未被覆盖；已有 `wechat-game` 工程保持不变。
